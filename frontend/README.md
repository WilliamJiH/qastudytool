# Frontend

Flutter web app for generating and viewing multiple-choice questions from backend notes analysis.

## Run (Web)

```bash
cd frontend
flutter pub get
flutter run -d chrome
```

Make sure backend is running on `http://localhost:8080`.
